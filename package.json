{
  "name": "arcns",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ arcns",
    "create-local": "graph create --node http://localhost:8020/ arcns",
    "remove-local": "graph remove --node http://localhost:8020/ arcns",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 arcns",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.37.0",
    "@openzeppelin/contracts": "^5.6.1",
    "@openzeppelin/contracts-upgradeable": "^5.6.1",
    "@openzeppelin/hardhat-upgrades": "^3.9.1",
    "dotenv": "^17.4.2",
    "hardhat": "^2.28.6"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "matchstick-as": "0.6.0"
  }
}
