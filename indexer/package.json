{
  "name": "arcns-subgraph",
  "version": "2.0.0",
  "description": "ArcNS v3 canonical subgraph — target: arcnslatest",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy arcnslatest"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
