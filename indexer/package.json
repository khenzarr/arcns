{
  "name": "arcns-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for Arc Name Service",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:studio": "graph deploy --studio arcns",
    "deploy:hosted": "graph deploy --product hosted-service arcns/arcns"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
