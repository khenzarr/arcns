{
  "name": "flashnames-subgraph",
  "version": "2.0.0",
  "description": "FlashNames canonical subgraph — legacy testnet target: arcnslatest",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy arcnslatest"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
